# ブランチ管理履歴

このファイルは、プロジェクト内のGitブランチの目的と管理情報を記録します。

## 📊 現在の状態（2026-01-23時点）

| ブランチ名 | どこに保存？ | 公開される？ | 何が入ってる？ | 今どっち？ |
|-----------|-------------|-------------|---------------|-----------|
| `main` | ・あなたのPC<br>・GitHub | ✅ はい<br>（GitHub Pagesで公開） | セキュリティ強化版<br>（これから作る） | ⭐ **今ここで作業中** |
| `backup-before-security-update` | あなたのPCのみ | ❌ いいえ<br>（ローカルだけ） | CDN使用版<br>（動作確認済み） | バックアップ用 |

**補足説明:**
- **main**: GitHub Pagesで一般公開される作業用ブランチ（URLを知ってる人はアクセス可能）
- **backup**: あなたのパソコンにだけ保存されている非公開のバックアップ

---

## 🔄 ブランチの切り替え方法（いつでも戻れます）

### ❌ 失敗した！元に戻したい場合

**バックアップに戻る手順:**

1. **Visual Studio Code（または任意のターミナル）を開く**
2. **以下のコマンドを実行:**
   ```bash
   git checkout backup-before-security-update
   ```
3. **確認メッセージが出たらOK:**
   ```
   Switched to branch 'backup-before-security-update'
   ```
4. **これで元の状態に戻りました！**
   - ファイルがすべて「セキュリティ強化前」の状態になっています
   - ブラウザで開くと、CDN版が動きます

---

### ✅ 作業を再開したい場合

**mainブランチに戻る手順:**

1. **Visual Studio Code（または任意のターミナル）を開く**
2. **以下のコマンドを実行:**
   ```bash
   git checkout main
   ```
3. **確認メッセージが出たらOK:**
   ```
   Switched to branch 'main'
   ```
4. **これで最新の作業状態に戻りました！**

---

### 💡 どのブランチにいるか確認する方法

**今どっちにいるか忘れた場合:**
```bash
git branch
```

**表示例:**
```
  backup-before-security-update
* main    ← この「*」マークが付いてるのが今いる場所
```

---

## 📁 各ブランチの詳細情報

### backup-before-security-update（バックアップ）

| 項目 | 内容 |
|------|------|
| **作成日** | 2026-01-23 |
| **作成理由** | セキュリティ強化実装前のバックアップ（失敗したときに戻るため） |
| **保存場所** | あなたのPCのみ（GitHubにプッシュしない） |
| **公開状況** | 非公開（あなただけが見られる） |
| **動作状態** | ✅ 正常動作確認済み |
| **技術的特徴** | ・外部CDN使用（cdnjs, unpkg, fonts.googleapis.com）<br>・CSPは緩い設定<br>・すべての機能が動く |

**このブランチはいつ使う？**
- セキュリティ強化版で問題が起きたとき
- 「やっぱり元に戻したい」と思ったとき
- 比較用に元のコードを見たいとき

---

### main（作業用・公開用）

| 項目 | 内容 |
|------|------|
| **用途** | 本番環境（GitHub Pagesで公開） |
| **保存場所** | あなたのPC + GitHub |
| **公開状況** | 公開（https://itohenthunagi.github.io/PDF-merger/） |
| **動作状態** | これから修正予定 |
| **技術的特徴** | ・ライブラリを同梱予定（vendor/ディレクトリ）<br>・CSPを厳格化予定<br>・外部通信なし |

**このブランチはいつ使う？**
- 通常の作業時（これから修正する）
- GitHub Pagesにデプロイするとき

---

## ⚠️ 重要な注意事項

1. **バックアップブランチはGitHubにプッシュしない**
   - `git push origin backup-before-security-update` は実行しないでください
   - あくまでローカルのみの保険です

2. **mainブランチだけGitHubにプッシュ**
   - セキュリティ強化版の作業が完了したら
   - `git push origin main` でGitHubに送信します

3. **いつでも戻れます**
   - 変更途中でも、コミット前でも、いつでもブランチ切り替え可能
   - 安心して作業を進めてください

---

## 📝 変更履歴

| 日付 | ブランチ | 操作 | 説明 | 作業者メモ |
|------|---------|------|------|-----------|
| 2026-01-23 | backup-before-security-update | 作成 | セキュリティ強化前のバックアップ作成 | CDN版を保存 |
| 2026-01-23 | main | 作業開始 | セキュリティ強化実装開始予定 | ライブラリ同梱版を作る |
